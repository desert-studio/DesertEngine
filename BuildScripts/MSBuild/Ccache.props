<!--
  MSVC compiler cache wiring.

  NO DOUBLE HYPHEN ANYWHERE IN THESE COMMENTS. XML forbids it inside a comment, and MSBuild then
  refuses to load the file for EVERY project on EVERY machine, switch or no switch: that is how the
  first CI run of this file (35863217377) failed all three legs in eight seconds.

  INERT unless DESERT_MSVC_CCACHE is set, so a developer's Visual Studio build is exactly
  what premake generated; CI's Windows job is what sets it (.github/workflows/ci.yml).

  WHY HERE AND NOT IN PREMAKE. premake 5.0-beta8 has no spelling for CLToolExe, CLToolPath,
  UseMultiToolTask or TrackFileAccess, and the switch is a property of the MACHINE doing the build (is
  a ccache binary installed, and where), not of the project.

  WHY IT IS IMPORTED EXPLICITLY AND NOT AS Directory.Build.props. A file of that name in the root is
  picked up by EVERY MSBuild project below it on every machine, including a developer's Visual Studio
  build and anything third party nested in the tree, and nothing in the command line says it happened.
  The Windows job names it instead: msbuild /p:CustomBeforeMicrosoftCommonProps=<this file>, which
  Microsoft.Common.props imports at the same point as a root Directory.Build.props, before the
  project body, so a build that does not pass the property never sees the file at all.

  NOT /p:ForceImportBeforeCppTargets. That was tried (6e1a17b1, d7ab8fa8) and it imports too late:
  CLToolPath still took effect, UseMultiToolTask did not, and every project went back to ONE cl.exe
  for all its sources. ccache counted 211 of 359 calls as "multiple source files" and hit once, where
  the Directory.Build.props position had given 2270 cacheable calls (runs 35984010180 against
  35961549096); Windows Release went from 78 to 104..146 minutes and started crossing its ceiling.
  The job now fails when ccache sees a multi-source call, so this cannot come back silently.

  WHAT EACH PROPERTY IS FOR, because none of them is optional:

  - CLToolExe / CLToolPath: MSBuild runs "$(CLToolPath)\$(CLToolExe)" instead of the real cl.exe. The
    directory holds ccache.exe COPIED TO THE NAME cl.exe, so ccache runs in masquerade mode and finds
    the real compiler on the PATH the C++ targets set up (skipping itself). Calling ccache.exe by its
    own name is not possible: the CL task cannot put the compiler's path in front of its arguments.

  - UseMultiToolTask: the default CL task hands EVERY source of a project to ONE cl.exe invocation,
    and ccache refuses any invocation with more than one input file ("multiple source files"), so
    without this the cache would see nothing at all and report it only in its statistics. The
    MultiToolTask runs one cl.exe per source instead. It is also the only intra-project parallelism
    this build has: without it Common -> Desert -> Editor, the chain every Windows build ends on,
    compiles on one core each (measured: Editor is the last 16 minutes of Windows Release, alone).

  - EnforceProcessCountAcrossBuilds: `msbuild -m` already runs one project per core, and each of
    those would now spawn one compiler per core too. This makes the MultiToolTasks of all nodes share
    one machine-wide count instead of multiplying (see the -m x /MP note in BuildScripts/Workspace.lua).

  - TrackFileAccess=false: file tracking injects a DLL into the compiler process tree to write .tlog
    files for incremental builds. A CI build is always clean, and the tracker would be following
    ccache's own cache writes as well.

  The /Zi -> /Z7 half is in Ccache.targets (CustomAfterMicrosoftCommonTargets): item metadata has to be
  overridden AFTER the project's own ItemDefinitionGroups.
-->
<Project>
  <PropertyGroup Condition="'$(DESERT_MSVC_CCACHE)' != ''">
    <CLToolExe>cl.exe</CLToolExe>
    <CLToolPath>$(DESERT_MSVC_CCACHE)</CLToolPath>
    <UseMultiToolTask>true</UseMultiToolTask>
    <EnforceProcessCountAcrossBuilds>true</EnforceProcessCountAcrossBuilds>
    <TrackFileAccess>false</TrackFileAccess>
  </PropertyGroup>
</Project>
